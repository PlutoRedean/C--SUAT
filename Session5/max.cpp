#include "max.h"
int max (int x, int y)
{
    if (x >= y) {
        return x;
    } else {
        return y;
    }
}
float max (float x, float y)
{
    if (x >= y) {
        return x;
    } else {
        return y;
    }
}